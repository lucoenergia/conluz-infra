# IP or hostname of the machine running the app role
APP_HOST=192.168.1.x

# Port exposed by conluz-api on the app machine
APP_API_PORT=8080

# Port exposed by conluz-web on the app machine
APP_WEB_PORT=80

# IP or hostname of the machine running the app role
APP_HOST=192.168.1.x
APP_API_PORT=8080
INFLUXDB_PORT=8086

# IP or hostname of the machine running the logging role
LOKI_HOST=192.168.1.x
LOKI_PORT=3100

# Alertmanager — SMTP email notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_FROM=alerts@example.com
SMTP_USERNAME=user@example.com
SMTP_PASSWORD=changeme
ALERT_RECEIVER_EMAIL=ops@example.com
